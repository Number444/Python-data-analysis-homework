# 五、思考题与课后作业（第 1、2 题解答）

---

## 1. 代数证明题

**题目：** 在一元线性回归中，证明残差之和必然等于零（$\sum_{i=1}^{n} e_i = 0$），且残差与自变量乘积之和也必然等于零（$\sum_{i=1}^{n} x_i e_i = 0$）。

### 模型设定

一元线性回归模型：

$$y_i = \beta_0 + \beta_1 x_i + \varepsilon_i$$

最小二乘估计通过最小化残差平方和（RSS）得到：

$$S(\beta_0, \beta_1) = \sum_{i=1}^{n} (y_i - \beta_0 - \beta_1 x_i)^2$$

记估计值为 $\hat{\beta}_0, \hat{\beta}_1$，拟合值 $\hat{y}_i = \hat{\beta}_0 + \hat{\beta}_1 x_i$，残差 $e_i = y_i - \hat{y}_i$。

### 证明

对 $S$ 分别求偏导并令其为零（一阶条件 / 正规方程）：

$$\frac{\partial S}{\partial \beta_0} = -2 \sum_{i=1}^{n} (y_i - \hat{\beta}_0 - \hat{\beta}_1 x_i) = 0$$

$$\frac{\partial S}{\partial \beta_1} = -2 \sum_{i=1}^{n} x_i (y_i - \hat{\beta}_0 - \hat{\beta}_1 x_i) = 0$$

**（1）证明 $\sum_{i=1}^{n} e_i = 0$：**

由第一个正规方程：

$$\sum_{i=1}^{n} (y_i - \hat{\beta}_0 - \hat{\beta}_1 x_i) = \sum_{i=1}^{n} (y_i - \hat{y}_i) = \sum_{i=1}^{n} e_i = 0$$

即残差之和为零。$\blacksquare$

**（2）证明 $\sum_{i=1}^{n} x_i e_i = 0$：**

由第二个正规方程：

$$\sum_{i=1}^{n} x_i (y_i - \hat{\beta}_0 - \hat{\beta}_1 x_i) = \sum_{i=1}^{n} x_i (y_i - \hat{y}_i) = \sum_{i=1}^{n} x_i e_i = 0$$

即残差与自变量乘积之和为零。$\blacksquare$

### 备注

- 这两个结论本质上是 OLS 一阶条件的直接推论，即**残差向量与设计矩阵的各列（常数列 1 与自变量列 x）正交**。
- 几何解释：$\hat{y}$ 是 $y$ 在由 $1$ 和 $x$ 张成的列空间上的正交投影，残差 $e$ 垂直于该空间。
- 前提：模型中**必须包含截距项**，否则 $\sum e_i = 0$ 不成立。

---

## 2. 理解思考题

**题目：** 为什么在多元回归中，往模型中添加一个纯随机生成的无关特征，$R^2$ 会上升或保持不变，而 $R^2_{adj}$ 会下降？请用自由度与 RSS 变动解释。

### 分析

**（1）为什么 $R^2$ 不下降？**

$R^2$ 的定义为：

$$R^2 = 1 - \frac{RSS}{TSS}$$

其中 TSS（总平方和）只取决于响应变量 $y$，与加入什么自变量无关。

OLS 是**最小化 RSS 的过程**。新增一个自变量后，原模型的所有拟合结果仍是新模型的"可行解"（只要令新变量系数为 0 即可还原旧模型）。因此新模型的最优 RSS **只会减小或不变**，绝不会增大：

$$RSS_{new} \le RSS_{old}$$

即使新特征是纯随机噪声，在有限样本中它与 $y$ 总会存在微小的"伪相关"，OLS 会利用这一点点相关性进一步压缩 RSS，所以 RSS 通常会**略微减小**，从而：

$$R^2_{new} \ge R^2_{old}$$

即 $R^2$ 会上升（或保持不变）。这正是 $R^2$ 的缺陷：**它无法区分变量是真有用还是噪声，加变量永不"吃亏"**。

**（2）为什么 $R^2_{adj}$ 会下降？**

调整 $R^2$ 的定义为：

$$R^2_{adj} = 1 - \frac{RSS/(n-p-1)}{TSS/(n-1)}$$

其中 $n$ 为样本量，$p$ 为自变量个数。加入一个无关变量后：

- **分子端**：$RSS$ 几乎不变（随机噪声只能带来极小的、由偶然性产生的下降）；
- **分母端（自由度惩罚）**：残差自由度从 $n-p-1$ 变为 $n-p-2$，**分母变小**，于是 $\dfrac{RSS}{n-p-1}$ **变大**。

因此：

$$R^2_{adj,new} = 1 - \frac{RSS_{new}/(n-p-2)}{TSS/(n-1)} < R^2_{adj,old}$$

**结论：** $R^2_{adj}$ 对每个新增变量施加了"自由度税"。只有当新变量带来的 RSS 下降幅度**超过**自由度损失造成的惩罚时，$R^2_{adj}$ 才会上升。纯随机无关特征对 RSS 的贡献期望几乎为零，抵不过惩罚，所以 $R^2_{adj}$ 下降。这也是实践中应当用 $R^2_{adj}$（而非 $R^2$）来比较不同变量个数模型的原因。

---
