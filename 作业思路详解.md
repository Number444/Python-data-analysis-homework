# 作业制作思路详解（给 Four 的快速上手指南）

> 本文档解释 `思考题1-2` 和 `思考题3` 两份作业的**解题思路、制作过程与关键细节**，帮助你在短时间内理解并能在课堂/答辩中复述。

---

## 一、第 1 题：代数证明题（残差之和为零）

### 题目要求
证明一元线性回归中：
- $\sum e_i = 0$（残差之和为零）
- $\sum x_i e_i = 0$（残差与自变量乘积之和为零）

### 核心思路（一句话版）
**这两个结论不是巧合，而是最小二乘法"一阶条件"的直接产物。** 整个证明只有三步：写目标函数 → 求偏导令其为零 → 发现偏导=0 的式子就是要证的东西。

### 推导链条

```
最小化目标：S(β₀, β₁) = Σ(yᵢ - β₀ - β₁xᵢ)²   ← 残差平方和 RSS
        │
        ├─ 对 β₀ 求偏导 = 0  →  -2Σ(yᵢ - β̂₀ - β̂₁xᵢ) = 0
        │                        括号里就是残差 eᵢ
        │                        → Σeᵢ = 0 ✔ 第(1)问证完
        │
        └─ 对 β₁ 求偏导 = 0  →  -2Σxᵢ(yᵢ - β̂₀ - β̂₁xᵢ) = 0
                                 → Σxᵢeᵢ = 0 ✔ 第(2)问证完
```

### 关键细节（容易被追问的点）
1. **"必然"二字的关键**：因为 OLS 估计量就是靠令偏导为零解出来的（正规方程），所以这两个等式是**结构性成立**，不依赖数据、不依赖假设检验。
2. **必须有截距项**：$\sum e_i = 0$ 依赖"对 β₀ 求偏导"这一步。如果模型没有截距，这条不成立。这是常见的陷阱考点。
3. **几何解释（加分项）**：残差向量 e 与设计矩阵的每一列（常数列 **1** 和自变量列 **x**）正交。$\sum e_i = 0$ 就是 e ⊥ **1**，$\sum x_i e_i = 0$ 就是 e ⊥ **x**。ŷ 是 y 在列空间上的正交投影。

---

## 二、第 2 题：理解思考题（R² vs 调整 R²）

### 题目要求
为什么往多元回归里加一个**纯随机无关特征**，R² 上升（或不变），而 R²_adj 下降？用自由度与 RSS 变动解释。

### 核心思路（一句话版）
**R² 是"加变量从不吃亏"的老好人，R²_adj 是"加变量要交税"的严厉考官。**

### 两条逻辑线

**线 1：R² 为什么不降？**
- $R^2 = 1 - RSS/TSS$，TSS 只看 y，与加什么变量无关 → 战场只剩 RSS。
- OLS 是最小化 RSS。新模型完全可以"假装新变量不存在"（令其系数 = 0），就退化成旧模型。
- 所以新模型的最优 RSS **至多等于**旧模型：$RSS_{new} \le RSS_{old}$，R² 永不下降。
- 随机噪声在有限样本里总会和 y 有一点"伪相关"，OLS 会利用这点相关性再压一点 RSS → R² 通常**微升**。

**线 2：R²_adj 为什么降？**
- $R^2_{adj} = 1 - \dfrac{RSS/(n-p-1)}{TSS/(n-1)}$
- 加一个变量：p → p+1，残差自由度 n-p-1 → n-p-2，**分母变小**。
- 而分子 RSS 几乎不动（噪声的贡献期望为零）。
- RSS 除以一个更小的数 → 整个分数变大 → 1 减去更大的数 → **R²_adj 下降**。

### 关键细节
- 比喻：R²_adj 对每个新变量收"自由度税"。新变量带来的 RSS 下降必须**跑赢税收**才让 R²_adj 上升；纯噪声跑不赢。
- 实践结论：**比较变量个数不同的模型，用 R²_adj，不用 R²**。这是这道题想让你记住的一句话。

---

## 三、第 3 题：Python 实操（Carseats 多元回归）

### 题目要求
Carseats 数据集，Sales ~ Price + Income + Advertising + ShelveLoc（定性），回答三小问：基准组 / ShelveLoc[Good] 系数含义 / VIF 共线性。

### 制作过程（实际执行的步骤）
1. **环境**：本机 Python 3.14，`pip install statsmodels pandas`。
2. **数据**：`sm.datasets.get_rdataset("Carseats", "ISLR")` 在线加载 ISLR 经典数据集，400 行。ShelveLoc 分布：Medium 219 / Bad 96 / Good 85。
3. **建模**：`smf.ols("Sales ~ Price + Income + Advertising + ShelveLoc")`——公式接口遇到字符串列**自动生成虚拟变量**，一行搞定。
4. **VIF**：从拟合好的模型里取设计矩阵 `model.model.exog`，逐列算 `variance_inflation_factor`。
5. 脚本存为 `03_python实操_carseats.py`，解答存为 `思考题3Python实操-Carseats多元回归.md`。

### 结果速记表

| 项目 | 数值 | 说明 |
|---|---|---|
| R² / R²_adj | 0.627 / 0.622 | 模型解释约 63% 的销售额变异 |
| F = 132.4, p ≈ 5e-82 | 整体极显著 | |
| Intercept | 10.39 | 基准：Price=Income=Adv=0 且 ShelveLoc=Bad |
| **ShelveLoc[T.Good]** | **+4.836** | 好货架位 vs 差货架位，多卖约 4.84 千件 |
| ShelveLoc[T.Medium] | +1.896 | 中等 vs 差，多卖约 1.90 千件 |
| Price | -0.057 | 价格每升 1 美元，销量降 57 件 |
| Income | +0.014 | 收入每升 1 千美元，销量升 14 件 |
| Advertising | +0.106 | 广告每升 1 千美元，销量升 106 件 |
| VIF | 全部 1.0~1.5 | 远低于警戒线 5，无共线性 |

### 三小问的答题要点
1. **基准组怎么判断？** 看报告里**缺了谁**。输出只有 `[T.Good]` 和 `[T.Medium]`，没有 Bad → Bad 被吸进截距，是基准组。
2. **ShelveLoc[Good] = 4.836 的含义**：控制住价格、收入、广告后，好位置比差位置平均多卖 4.84 千件。换算一下更有冲击力：相当于广告预算增加 4.836/0.1056 ≈ 46 个单位的效果 → **货架位比砸广告值钱**。
3. **VIF 怎么看**：>5（严格）或 >10（宽松）才算有共线性。全在 1.5 以下 → 变量间几乎无关，系数可信。

### 关键细节
- ShelveLoc 是**字符串列**（Bad/Medium/Good），statsmodels 公式接口自动按字母序选 Bad 为基准——不用手动 get_dummies。
- Sales 单位是**千件**，说"多卖 4.84 千件"比"多 4.84 个单位"更专业。
- 虚拟变量也有 VIF（1.49 左右），略高于 1 是正常的（同类虚拟变量天然相关），不用紧张。

---

## 四、文件清单

| 文件 | 内容 |
|---|---|
| `思考题1残差证明-思考题2R2与调整R2.md` | 第 1、2 题完整解答 |
| `思考题3Python实操-Carseats多元回归.md` | 第 3 题完整解答 |
| `03_python实操_carseats.py` | 第 3 题可运行脚本（结果已实跑验证） |
| `作业思路详解.md`（本文件） | 三题思路汇总 |
